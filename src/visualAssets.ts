/**
 * Optional hero-only star photograph.
 *
 * Put the selected image in `public/hero-stars.jpg`, then change this value to
 * `'/hero-stars.jpg'`. Leave it as `null` to use the lightweight CSS fallback.
 */
export const HERO_STAR_BACKGROUND: string | null = null;
